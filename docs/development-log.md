# 개발 로그

## 작성 규칙

- 이 문서에는 날짜별 개발 내역만 기록한다.
- 각 날짜는 `## YYYY-MM-DD` 형식으로 작성한다.
- 날짜 아래에는 작업 단위를 `### 기능명` 형식으로 작성한다.
- 각 기능 섹션에는 실제로 개발한 내용만 bullet로 남긴다.
- 구현 의도, 회고, TODO, 잡담은 이 문서에 쓰지 않는다.

## 2026-05-12

### 프로젝트 컨텍스트 문서화

- `docs/context/project-overview.md` 문서를 추가하고 과제 원문 기준 필수 요구사항과 추가 요구사항을 구분해 정리했다.
- 크리에이터 정산 API의 배경 시나리오, 필수 구현 범위, 정산 규칙, 샘플 데이터 검증 기준을 문서화했다.

### 도메인 설계 초안 정리

- `docs/context/domain-design.md` 문서를 추가하고 `Creator`, `Admin`, `Course`, `SaleRecord`, `CancellationRecord` 기준의 도메인 구조를 정리했다.
- 로그인 요구를 반영해 크리에이터와 운영자 계정 모델, 인증/인가 방향, 정산 서비스 책임을 문서화했다.

### ERD 문서 작성

- `docs/context/erd.md` 문서를 추가하고 저장 대상 테이블을 표 형식으로 정리했다.
- `creator`, `admin`, `course`, `sale_record`, `cancellation_record`의 컬럼, 관계, 제약조건을 명시했다.

### 개발 규칙 문서 작성

- `docs/context/development-rules.md` 문서를 추가하고 실무 적합형 클린 아키텍처 기준의 계층 의존 규칙을 정리했다.
- `Controller -> UseCase -> Service -> Repository` 구조, `Request/Command/Result/Response` 전달 규칙, 트랜잭션 위치 원칙을 문서화했다.
- 순환 참조 방지를 위해 `Service`가 다른 `Service`를 의존하지 않는 규칙을 추가했다.

### API 명세 문서 작성

- `docs/context/api-spec.md` 문서를 추가하고 인증, 판매, 취소, 정산 API의 요청/응답 구조를 정리했다.
- 공통 응답 형식과 프로젝트 기준 에러 코드 체계를 문서화했다.

### 프로젝트 기본 설정 구성

- Swagger, JPA, MySQL, JWT, Validation, Flyway 의존성을 `build.gradle`에 추가했다.
- `SecurityConfig`, `SwaggerConfig`, `application.yaml`을 정리해 보안, 문서화, 데이터베이스 연결의 기본 구성을 잡았다.

### 커밋 스킬 정리

- 프로젝트 전용 Codex 커밋 스킬을 로컬 `.codex/skills/commit` 경로로 정리했다.
- 스킬 이름을 `commit`으로 변경하고 저장소 커밋 규칙에 맞는 헤더와 본문 작성 흐름을 정리했다.
- 커밋 수행 시 `docs/development-log.md`를 함께 갱신하도록 스킬 규칙을 추가했다.

### 공통 응답 및 예외 처리 추가

- `ApiResponse` 공통 응답 객체를 정리하고 성공/실패 응답 생성 메서드를 추가했다.
- `ErrorCode`, `BusinessException`, `GlobalExceptionHandler`를 연결해 공통 예외 응답 처리 기반을 구성했다.
- 입력 검증, 요청 파라미터, JSON 파싱, 비즈니스 예외를 일관된 응답 형식으로 반환하도록 정리했다.

### JWT 인증 기반 추가

- `JwtProvider`, `JwtProperties`, `JwtAuthenticationFilter`를 추가해 액세스 토큰 생성 및 검증 기반을 구성했다.
- 크리에이터와 운영자 인증 주체를 구분하는 `AuthenticatedPrincipal`, 전용 `UserDetailsService`, 인증 프로바이더 구조를 추가했다.
- 보안 예외 응답 처리를 위해 `SecurityResponseWriter`와 인증 관련 에러 코드를 정리하고 설정 값을 `application.yaml`에 반영했다.

### Flyway 초기 스키마 추가

- `src/main/resources/db/migration/V1__init_schema.sql` 파일을 추가했다.
- `creator`, `admin`, `course`, `sale_record`, `cancellation_record` 테이블과 기본 인덱스, 제약조건을 초기 마이그레이션으로 구성했다.

### API 명세 보정

- 판매 등록 요청에서 `saleId`, 취소 등록 요청에서 `cancelId`를 제거했다.
- 등록 식별자는 클라이언트 입력이 아니라 서버 생성 값으로 정리했다.

### 크리에이터/운영자 로그인 구현

- `api/auth`, `service/auth`, `repository` 구조를 추가해 크리에이터와 운영자 로그인 흐름을 구현했다.
- 로그인 요청/응답 DTO, 인증 컨트롤러, 유스케이스, 서비스, 저장소를 연결했다.
- 보안 설정과 JWT 인증 구조를 로그인 API 기준으로 보완했다.

### 초기 계정 시드 추가

- `Seeder`를 추가해 크리에이터와 운영자 초기 계정을 적재하는 기반을 구성했다.
- 인증 테스트와 로그인 흐름 확인에 필요한 기본 계정 데이터를 애플리케이션 실행 시점에 준비하도록 정리했다.

### 권한 정책 반영

- `SecurityConfig`에서 판매 등록과 취소 등록은 현재 단계에서 인증 없이 허용하도록 설정했다.
- 판매 내역 조회는 `ROLE_CREATOR`, 운영자 정산 조회는 `ROLE_ADMIN` 기준으로 접근 제어를 분리했다.

### 개발 규칙 단순화

- `Command`, `Result`를 강제하는 규칙을 제거하고 과도한 보일러플레이트를 줄이는 방향으로 문서를 수정했다.
- `Service`에는 웹 DTO를 직접 넘기지 않고, 필요한 값 또는 최소한의 서비스 전용 DTO만 사용하도록 정리했다.

### 판매/취소 API 구현 시작

- `api/sales`, `service`, `repository`, `domain` 구조를 추가해 판매 등록, 취소 등록, 판매 조회 기능의 기본 흐름을 구성했다.
- `SaleRecord`, `Course`, `CancellationRecord` 엔티티와 관련 저장소를 연결했다.
- 판매/취소 권한 정책과 API 문서, 개발 규칙을 현재 구현 방향에 맞게 보정했다.

### 커밋 스킬 한국어 기준 보정

- 커밋 스킬이 기본적으로 한글 헤더와 본문을 작성하도록 규칙과 예시를 수정했다.

### 에러 코드 보정

- 크리에이터 조회 실패 상황에 사용할 `NOT_FOUND_CREATOR` 에러 코드를 추가했다.
- API 명세의 인증 에러 코드 설명도 현재 코드 기준에 맞게 정리했다.

### 판매 조회 및 취소 등록 보강

- 판매 조회 API에 기간 조건과 인증 주체 기반 조회 흐름을 연결했다.
- 판매 취소 등록 API에 `saleId` path variable 기반 취소 생성 흐름을 연결했다.
- 판매/취소 DTO 이름과 서비스, 저장소 조회 로직을 현재 구현 방향에 맞게 정리했다.

## 2026-05-13

### 샘플 시드 데이터 추가

- `V2__insert_seed_data.sql`에 샘플 `course`, `sale_record` 데이터를 추가했다.
- 샘플 판매 데이터는 문서에 정의한 검증 시나리오 기준의 결제 일시와 금액을 반영했다.

### 초기 스키마 제약 보정

- 엔티티의 `ConstraintMode.NO_CONSTRAINT` 설정과 맞추기 위해 `V1__init_schema.sql`의 외래키 제약을 제거했다.
- 컬럼 구조와 조회용 인덱스는 유지하고 스키마 제약만 완화했다.

### 정산 조회 계산 로직 정리

- 정산 조회의 기간 경계 생성을 `SettlementUseCase`에서 일관되게 처리하도록 정리했다.
- 판매/취소 합계 계산을 내부 공통 메서드와 `SettlementMetrics` record로 묶어 중복을 줄였다.
- 정산 조회 서비스는 `LocalDateTime` 범위를 직접 받아 조회 책임만 가지도록 정리했다.

### 날짜 유틸 공통화

- `common/util/DateTimeUtil`을 추가해 월 시작, 월 종료 exclusive, 일 시작, 일 종료 exclusive 계산을 공통 메서드로 분리했다.
- `SettlementUseCase`의 날짜 경계 계산 로직을 유틸 메서드 호출로 정리했다.

### API 에러 코드 정리

- Bean Validation과 바인딩으로 처리할 수 있는 입력 오류 코드를 API 명세에서 제거했다.
- 입력 오류는 `COMMON_001`, 비즈니스/인증 오류는 개별 에러 코드로 유지하도록 문서를 정리했다.

### 정산 조회 API 구현

- `SettlementController`, `SettlementUseCase`, 정산 응답 DTO를 추가해 크리에이터 월별 정산 조회와 운영자 정산 집계 조회 흐름을 구현했다.
- 내부 집계용 `SettlementMetrics`와 날짜 유틸을 활용해 정산 계산 로직과 기간 경계 처리를 공통화했다.

### 인증/인가 에러 응답 정리

- `CustomAuthenticationEntryPoint`, `CustomAccessDeniedHandler`를 추가해 인증 실패와 권한 부족 상황을 공통 응답 형식으로 반환하도록 정리했다.
- `SecurityConfig`에 예외 처리 핸들러를 연결하고 인증 필요, 권한 없음 에러 코드를 보완했다.
- JWT 필터에서 토큰 예외 발생 시 `SecurityContext`를 비우고 인증 에러 응답만 남기도록 정리했다.

### 존재하지 않는 엔드포인트 404 처리 정리

- `NoHandlerFoundException`을 공통 예외 처리기에 연결해 존재하지 않는 경로 요청을 공통 응답 형식으로 반환하도록 정리했다.
- `application.yaml`에 핸들러 미존재 예외 전환 설정을 추가하고 기본 정적 리소스 매핑은 비활성화했다.
- `SecurityConfig`에서 보호 대상을 실제 구현된 API 경로만 명시하고 나머지 요청은 `permitAll()`로 열어 404 응답이 보안 규칙보다 먼저 막히지 않도록 정리했다.

### 허용되지 않은 HTTP 메서드 405 처리 정리

- `HttpRequestMethodNotSupportedException`을 공통 예외 처리기에 연결해 잘못된 HTTP 메서드 요청을 공통 응답 형식으로 반환하도록 정리했다.
- `COMMON_004` 에러 코드를 추가해 405 응답도 다른 예외와 같은 포맷으로 처리하도록 맞췄다.

### 날짜 입력 검증 메시지 정리

- 판매 등록과 취소 등록 요청의 `OffsetDateTime` 필드에 JSON 날짜 형식을 명시하고 필수값 검증 메시지를 보강했다.
- 조회 API의 `LocalDate`, `YearMonth` 파라미터에 필수값 메시지를 추가하고 날짜 형식 변환 실패 시 입력 형식을 안내하는 예외 처리를 보강했다.

### 무료 강의 금액 검증 기준 보정

- 판매 금액과 환불 금액은 무료 강의와 무료 환불 케이스를 허용하도록 `0 이상` 검증으로 조정했다.
- 금액 검증 메시지도 제약값과 일치하도록 함께 수정했다.

### KST 기준 일시 저장 로직 보정

- 판매 등록과 취소 등록 시 `OffsetDateTime` 입력값을 바로 `LocalDateTime`으로 버리지 않고 `Asia/Seoul` 기준으로 정규화한 뒤 저장하도록 정리했다.
- `DateTimeUtil`에 KST 변환 메서드를 추가해 정산 기준 시간대 정책을 코드에서 일관되게 유지하도록 맞췄다.

### 컨텍스트 문서 규칙 보정

- 프로젝트 개요 문서에 KST 기준 입력 일시를 내부 `LocalDateTime`으로 정규화해 저장한다는 원칙을 명시했다.
- API 명세 문서에 남아 있던 `Service`의 `Command/Result` 구현 기준 문구를 제거하고 현재 개발 규칙과 맞게 정리했다.

### 정산 상태 관리 설계 반영

- `domain-design` 문서에 `Settlement` 엔티티와 `SettlementStatus(PENDING, CONFIRMED, PAID)` 기준의 상태 관리 흐름을 추가했다.
- `erd` 문서에 `settlement` 테이블과 정산 상태, 금액 스냅샷, 생성/확정/지급 시각, 유니크 제약과 상태 전이 규칙을 반영했다.

### 정산 예상 조회와 실제 정산 관리 역할 분리

- 크리에이터 월별 정산 조회는 `Settlement` 조회가 아니라 원천 판매/취소 데이터를 기준으로 계산하는 예상 정산 조회라는 점을 문서에 명확히 반영했다.
- `Settlement`는 크리에이터 정산 생성 이후 관리자 확인, 확정, 지급 상태를 관리하는 실제 정산 데이터라는 점을 설계 문서와 ERD에 맞게 정정했다.

### 도메인 설계 문서 구조 정리

- `domain-design` 문서에서 `SettlementPolicy`, `SettlementService`, `UseCase` 같은 구현 계층 중심 설명을 제거했다.
- 엔티티, 상태, 관계, 정산 규칙, 정산 생성과 상태 전이 흐름 중심으로 문서를 다시 정리했다.

### 중복 정산 방지와 수수료율 이력 설계 보강

- `domain-design`에 동일 `creatorId + yearMonth` 조합 중복 정산 방지 규칙과 정산 생성 시점 유효 수수료율 적용 규칙을 추가했다.
- `erd`에 `settlement`의 수수료율 스냅샷 컬럼과 `fee_policy_history` 테이블을 추가하고, 과거 정산이 당시 수수료율을 유지하도록 설계를 보강했다.

### 월별 단일 수수료율 정책 명시

- 과제 원문의 `순 판매 금액 = 총 판매 - 환불`, `플랫폼 수수료 = 순 판매 금액의 수수료율%` 공식을 유지하기 위해 수수료율은 각 정산 대상 월마다 하나만 존재하는 것으로 해석했다.
- `FeePolicyHistory`를 기간 중간 변경 이력이 아니라 월별 수수료율 이력 구조로 정리하고, 관련 설계 문서와 ERD 표현을 이에 맞게 수정했다.

### 정산 관리 API 명세 추가

- `api-spec`에 크리에이터 정산 생성, 생성된 정산 목록/상세 조회, 운영자 정산 조회/확정/지급 처리 API를 추가했다.
- 수수료율 이력 등록/목록/상세 조회 API와 중복 정산, 월 종료 전 생성 금지, 상태 전이 충돌 관련 에러 코드를 함께 정리했다.

### Settlement 연월 컬럼명 보정

- MySQL 마이그레이션 구문 오류를 피하기 위해 `settlement.year_month` 컬럼명을 `settlement_year_month`로 변경했다.
- `Settlement` 엔티티 매핑과 ERD 문서의 정산 유니크 제약 설명도 새 컬럼명 기준으로 정리했다.

### 판매/취소 등록 API 식별자 정책 보정

- 과제 검증용 데이터 입력 API라는 성격에 맞게 판매 등록과 취소 등록 요청에서 `saleId`, `cancelId`를 직접 받는 것으로 API 명세를 수정했다.
- 중복 식별자 입력 시 반환할 `SALE_002`, `CANCELLATION_002` 에러 코드도 함께 문서에 추가했다.

### 취소 시각 검증 보강

- 취소 등록 시 `canceledAt`이 원본 판매의 `paidAt`보다 빠를 수 없도록 검증을 추가했다.
- 관련 에러 코드 `CANCELLATION_003`과 API 명세 문구를 함께 정리했다.

## 2026-05-14

### 판매 수수료율 스냅샷 기반 정산 구현

- 판매 등록 시 현재 수수료율을 `SaleRecord`에 저장하도록 정산 기준을 변경했다.
- 정산 계산을 원본 판매 수수료율 기준의 수수료율별 순액 합산 방식으로 수정했다.
- 정산, 수수료율 정책, API 명세와 Flyway 마이그레이션을 새 정책에 맞게 정리했다.

### 시나리오 테스트용 시드 추가

- `V4__scenario_test_seed.sql`을 추가해 무료 강의, 다중 취소 누적, 월 경계, 혼합 수수료율, 정산 상태 조회 시나리오를 검증할 수 있는 데이터를 구성했다.
- SQL 주석으로 각 데이터 묶음이 어떤 시나리오를 검증하기 위한 것인지 바로 읽히도록 정리했다.

### 초기 계정 시드를 Flyway로 이전

- 제출 환경에서도 동일하게 초기 데이터가 들어가도록 `V2__insert_seed_data.sql`에 크리에이터와 운영자 계정 시드를 추가했다.
- 기존 `Seeder`와 같은 원문 비밀번호 기준의 BCrypt 해시를 사용해 로그인 검증 흐름이 마이그레이션 기반 데이터와도 맞도록 정리했다.

### 시나리오 시드 계정 해시 정리

- `V4__scenario_test_seed.sql`의 시나리오용 크리에이터 계정도 `encoded-password` 대신 V2와 동일한 BCrypt 해시를 사용하도록 수정했다.
- 시나리오 시드만 적용한 환경에서도 로그인 검증을 바로 할 수 있도록 계정 데이터 형식을 맞췄다.

### 무료 금액 시드와 초기 체크 제약 정합성 보정

- `V4` 시나리오 시드의 무료 강의/무료 환불 데이터가 초기 스키마 체크 제약과 충돌하지 않도록 `V1__init_schema.sql`의 금액 제약을 `0 이상` 기준으로 수정했다.
- 현재 문서와 검증 정책이 무료 금액을 허용하는 방향과 일치하도록 스키마 제약 이름과 조건을 함께 정리했다.

### 시나리오 테스트 시드 독립성 보정

- `V4__scenario_test_seed.sql`의 각 시나리오가 서로 다른 크리에이터 기준으로 조회되도록 데이터를 분리했다.
- 무료 강의, 다중 취소, 월 경계, 혼합 수수료율, 정산 상태 조회를 다른 시나리오 데이터와 섞이지 않게 재구성했다.

### Swagger 명세 보강

- 각 컨트롤러 Docs 인터페이스에 태그, 엔드포인트 설명, 요청 파라미터 설명, 주요 응답 코드를 OpenAPI 어노테이션으로 추가했다.
- 인증, 판매, 정산, 수수료율 관련 요청/응답 DTO 클래스와 필드에 설명과 예시값을 추가해 Swagger 화면에서 데이터 구조를 바로 이해할 수 있도록 정리했다.

### README 초안 작성

- 프로젝트 개요, 기술 스택, 요구사항 해석, 설계 결정, 제약사항, AI 활용 범위, API 목록, 데이터 모델, 테스트 검증 방식 기준의 `README.md` 초안을 작성했다.
- 실행 방법 섹션은 추후 실제 실행 가능 상태에 맞춰 보완할 수 있도록 자리만 남겼다.

### 도커 실행 환경 초안 구성

- 채점자가 MySQL과 애플리케이션을 함께 실행할 수 있도록 `docker/Dockerfile`, `docker/docker-compose.yml`, `docker/.env`를 추가했다.
- 애플리케이션 이미지는 Gradle 빌드 결과물을 멀티 스테이지로 생성하고, Compose는 MySQL healthcheck 이후 애플리케이션이 기동되도록 구성했다.
- 불필요한 빌드 컨텍스트 전송을 줄이기 위해 루트 `.dockerignore`도 함께 추가했다.

### Service / UseCase 단위 테스트 추가

- `src/test/java` 아래에 인증, 수수료율, 판매, 취소, 정산 관련 Service와 UseCase 단위 테스트를 추가했다.
- 테스트 fixture 유틸을 별도로 두고, repository와 controller를 제외한 계층을 mock 기반으로 검증할 수 있도록 구성했다.

### 날짜 범위 검증 공통화

- `DateTimeUtil`에 `LocalDate`, `YearMonth` 범위 검증 메서드를 추가하고 판매 조회와 정산 조회 유스케이스에서 공통 사용하도록 정리했다.
- `from > to` 계열 검증은 `COMMON_005` 공통 에러코드 하나로 통일하고 관련 테스트와 API 문서도 함께 수정했다.

### 과거 판매 수수료율 유지 시나리오 추가

- 4월에 서로 다른 수수료율로 판매된 2건이 5월과 6월에 각각 취소되는 시나리오를 `V4__scenario_test_seed.sql`에 추가했다.
- 5월과 6월의 현재 수수료율 이력은 별도 값으로 두고도, 취소 정산은 원본 판매의 수수료율을 따라가야 하는 검증 케이스를 분리했다.

### 수수료율 즉시 적용 정책으로 문서 정리

- 수수료율 변경은 다음 달 반영이 아니라 변경 시점부터 즉시 적용되는 것으로 정책을 정리했다.
- `FeePolicyHistory`는 월 기준 이력이 아니라 변경 시점 기준 이력으로 설명을 바꾸고, 판매 당시 수수료율 스냅샷이 정산 계산의 직접 기준임을 문서 전반에 명시했다.

### 음수 수수료와 음수 정산 예정 금액 정책 문서화

- 플랫폼 수수료는 음수가 될 수 없으므로 최소 0원으로 처리한다는 정책을 정산 규칙 문서에 추가했다.
- 정산 예정 금액이 음수인 경우는 이후 정산 이월이 아니라 해당 정산 결과에서 직접 반영되는 차감 조정 금액으로 해석하도록 문서를 정리했다.

### FeePolicyHistory 시점 기준 이력으로 구조 변경

- `FeePolicyHistory`를 기준 연월(`targetYearMonth`)이 아니라 실제 적용 시작 시각(`effectiveStartedAt`)을 저장하는 구조로 변경했다.
- 관련 엔티티, repository, 서비스, DTO, 수수료율 이력 API, Flyway 마이그레이션과 시나리오 시드를 모두 시점 기준 이력으로 정리했다.

### 시나리오 시드와 수수료율 이력 정합성 보정

- `V4__scenario_test_seed.sql`의 수수료율 이력을 즉시 적용 정책에 맞게 시점 기준으로 다시 정리했다.
- 혼합 수수료율 및 과거 판매 수수료율 유지 시나리오의 판매 수수료율이 실제 이력 시각과 일치하도록 판매 데이터도 함께 보정했다.

### 시나리오 6과 7 정산 테스트 케이스 보강

- 시나리오 6에 5월 신규 판매를 추가해 과거 판매 환불과 같은 월 신규 판매가 함께 있을 때 수수료가 0원이 아닌 경우를 검증할 수 있게 했다.
- 시나리오 7을 별도로 추가해 환불만 있는 월에 플랫폼 수수료가 0원으로 처리되고 정산 예정 금액이 음수가 되는 케이스를 독립적으로 확인할 수 있게 했다.

### 시나리오 8 추가

- 플랫폼 수수료는 양수지만 환불이 더 커서 정산 예정 금액이 음수가 되는 케이스를 `V4__scenario_test_seed.sql`에 추가했다.
- 해당 시나리오는 같은 월의 신규 판매로 양수 수수료를 만들고, 이전 월 판매 환불로 순매출을 더 크게 음수로 만드는 구조로 분리했다.

### 시나리오 시드와 수수료율 이력 재정합화

- `fee_policy_history`가 전역 현재 수수료율 변경 이력이라는 전제에 맞게, 일부 시나리오 판매 데이터의 `fee_rate_percent`를 실제 변경 시각과 일치하도록 보정했다.
- 월 경계 시나리오의 4월 말 판매 데이터도 당시 유효 수수료율 이력과 맞도록 `25%`로 수정했다.

### 계산 시나리오 추가 보강

- 판매만 존재하는 순수 정산, 순매출이 정확히 0원이 되는 정산, 수수료 소수점 올림, 여러 수수료율 판매와 환불이 섞인 복합 정산 시나리오를 `V4__scenario_test_seed.sql`에 추가했다.
- 새 시나리오에 필요한 7월 수수료율 변경 이력도 함께 추가하고, 모든 판매 데이터의 수수료율이 이력 시각과 일치하는지 기준을 맞췄다.

### 시나리오 검증 가이드 문서 추가

- `docs/scenario-verification-guide.md`를 추가해 각 계산 시나리오별 로그인 계정, 호출 API, 기대 결과를 한 번에 확인할 수 있도록 정리했다.
- 채점자가 시드 데이터와 실제 응답 기대값을 빠르게 대조할 수 있게 정산 금액과 의미를 함께 문서화했다.

### 음수 수수료 0원 처리 정책 반영

- 환불만 있는 월처럼 수수료율별 순액이 음수가 되는 경우에도 플랫폼 수수료는 음수가 되지 않도록 정산 수수료 계산을 보정했다.
- 이제 정산 수수료는 수수료율별 순액이 0 이하이면 0원으로 처리되고, 음수 정산 예정 금액은 해당 정산의 차감 조정 금액으로만 반영된다.

### 에러코드 사용 현황 기준 재정렬

- `api-spec`에서 현재 구현에서 사용하지 않는 에러코드를 제거했다.
- `CANCELLATION` 에러코드는 `001`부터 다시 정렬하고, `SETTLEMENT` 에러코드는 `001~005` 범위로 다시 정렬했다.
- 실제 `ErrorCode` 문자열 값과 문서의 코드 표를 같은 번호 체계로 맞췄다.

### 수수료율 변경 이력 등록 API 복구

- 판매 등록 API가 `paidAt`을 직접 받는 현재 구조에서는 과거 시점 판매 테스트를 위해 수수료율 변경 이력 등록 API도 함께 제공하는 편이 더 일관적이라고 판단했다.
- `POST /admin/fee-policy-histories`를 다시 복구하고, 관련 DTO, 컨트롤러, 유스케이스, Swagger 명세와 문서 설명을 함께 원복했다.
- 보안 설정에도 수수료율 변경 및 이력 조회/등록 경로를 `ROLE_ADMIN` 권한으로 다시 연결했다.

### 강의 등록 API 추가

- 테스트용 원천 데이터 입력을 위해 `POST /courses` 강의 등록 API를 추가했다.
- `Course` 엔티티와 스키마에 `registeredAt` 필드를 추가하고, 기존 시드 데이터도 함께 보정했다.
- 강의 ID 중복 등록은 `COURSE_002` 에러코드로 검증하도록 정리했다.

### 판매 일시 검증 에러코드 추가

- 판매 등록 시 결제 일시가 강의 등록 일시보다 빠를 수 없는 규칙을 위해 `SALE_003`을 추가했다.
- API 명세의 SALE 에러코드 표에도 같은 내용을 반영했다.
## 2026-05-15
- 이전 Flyway 마이그레이션 직접 수정 사항을 `V5__add_course_registered_at_and_initial_fee_policy_history.sql`로 분리했다.
- `CourseService` 판매 등록 검증 흐름에 맞게 `CourseServiceTest`, `SaleUseCaseTest`를 보정했다.
- 서비스와 유스케이스의 예외 흐름을 보강하는 단위 테스트를 추가했다.
- 운영자 예상 정산 집계에서 정산 예정 금액이 0원인 크리에이터도 목록에 포함되도록 조정했다.
- 정산 중복 검증을 `SettlementService.createSettlement(...)` 내부 책임으로 이동했다.
- 유니크 제약 기반 생성 API에서 `DataIntegrityViolationException`을 비즈니스 예외로 변환하고, 저장 시점 충돌 테스트를 추가했다.
- 문서 전반을 현재 구현 기준으로 다시 정렬했다.
- 판매 등록 수수료율 결정 방식을 `paidAt` 기준 수수료율 이력 조회로 수정해 `project-overview`, `domain-design`, `erd`, `api-spec`, `README`에 반영했다.
- 취소 등록 동시성 처리를 위해 원본 `SaleRecord` 비관적 쓰기 락을 사용하는 이유와, 유니크 제약 기반 생성 API의 DB 예외 변환 이유를 관련 문서에 정리했다.
- README에 Docker Compose 기반 실행 방법, 접속 주소, 기본 계정, 종료 및 초기화 방법을 추가했다.
- `GET /sales`의 `from`, `to` 조회 파라미터를 구현과 맞게 API 명세에서 필수값으로 정정했다.
- 수수료율 변경 API와 수수료율 이력 등록 API의 역할 차이를 문서에 명시하고, 시나리오 검증 가이드를 로그인/호출 순서/확인 포인트까지 포함하도록 상세화했다.
- `V4` 시드에 기존 시나리오와 충돌하지 않는 미래 시점 수수료율 이력(`2025-08-01 / 20%`)을 추가하고, 현재 `fee_policy`와 마지막 이력 값의 정합성을 맞췄다.
- README의 테스트 실행 방법을 단위 테스트, 시드 기반 시나리오 테스트, 직접 데이터 입력 테스트의 3가지 방식으로 재구성했다.
- 시나리오 검증 가이드의 로그인 예시를 실제 시드 로그인 ID 형식(`creator_scenario_*`)에 맞게 수정했다.
- Docker Compose에 MySQL healthcheck와 앱의 `service_healthy` 의존을 추가해 초기 기동 안정성을 보강했다.
